import { StyleSheet } from "react-native";

const globalStyle = StyleSheet.create({
    textInput: {
        width: '100%',
        padding: 10,
        marginTop:5,
        marginBottom: 5,
        borderWidth: 1,
        borderColor: '#1F1F1F',
    },
    heading: {
        fontSize: 30,
        fontWeight: "bold",
    }
});

export default globalStyle;